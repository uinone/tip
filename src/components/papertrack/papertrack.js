import infos from "../../infos/infos.json";
import Card from "../card/card";

const PaperTrack = () => {
    return (
        <div className="section" style={{
            display: "flex",
            flexDirection: "column",
            width: "55%",
            boxSizing: "border-box",
        }}>
            <h2>Paper Track</h2>
        </div>
    );
};

export default PaperTrack;